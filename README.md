# cuLearn

GPU-accelerated classical ML algorithms built with PyTorch.

## Install (dev)
```bash
pip install -e ".[dev,test]"
```
## Quick Start
```python
import torch
import culearn
print(culearn.__version__)
```
## Roadmap
- StandardScaler
- KMeans
- kNN (classification/regression)